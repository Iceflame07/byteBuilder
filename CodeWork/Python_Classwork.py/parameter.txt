'''
Def the function that will enter parameter and save it
Print the parameter in reversed order and save it
'''

def parameter(Cohort):
	cohort = temp
	x = 1
	temp = x
	cohort = 1
	cohort = temp print(cohort, end ' ')