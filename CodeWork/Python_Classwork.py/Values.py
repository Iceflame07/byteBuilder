'''
Prompt the user to input numbers and save
Prompt the computer to iterate through the list of number until the user stops and save
Prompt the computer to break the list of number if a string is entered and save
'''

scores = int(input("Enter scores:"))
numbers != int(input("numbers:"))
numbers = scores

for numbers in range(len(scores)):
	userinput += scores.extend(10)

userinput += numbers
scores.insert(10)
print(scores)
